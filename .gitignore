node_modules/
.expo/
dist/
web-build/

# CNG native output — generated by `expo prebuild` locally or on EAS; never committed.
# The native sources of truth are app.json, targets/, and modules/.
/ios/
/android/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Environment variables
.env
.env.*
!.env.example

# Jest transform cache (persisted in CI via actions/cache)
.jest-cache/

# Claude Code session tooling artifacts
.agents/
skills-lock.json

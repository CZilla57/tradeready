{
  "name": "tradeready-backend-workers",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir=dist"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "stripe": "^16.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
